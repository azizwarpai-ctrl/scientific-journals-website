# Infrastructure Discovery — August 2026

Read-only SSH audit of both production hosts, performed 2026-08-08 as Phase 0 of the
system-overhaul plan. Commands were non-mutating; no host state was changed.

## Host inventory

| Surface | Host | SSH alias | Stack |
| --- | --- | --- | --- |
| digitopub.com (this repo) | Hostinger, `147.93.48.207`, user `u391496830` | `hostinger` | Next.js 16 standalone under Passenger/LiteSpeed |
| journals.digitopub.com / submitmanager.com (OJS) | SiteGround, user `u1135-vfzcqxwgpglc` | `submitmanager.com` | OJS 3.5.0.2, PHP 8.2.33, MySQL 8.4.10 |

## Hostinger (digitopub.com)

### Runtime
- **Node 20.19.4** — but ONLY via Passenger: `PassengerNodejs /opt/alt/alt-nodejs20/root/bin/node`.
  The interactive SSH `$PATH` has **no `node`, `npm`, or `npx`** (confirms the
  `docs/hostinger_deployment_report.md` finding). **`bun` 1.3.14 IS installed at `/usr/bin/bun`.**
- The repo previously pinned Node 18 (`.nvmrc`, `.node-version`, `engines`); production runs 20.
  Fixed in this phase — pinned to 20.19.4.

### Process model (dictates deploy/restart strategy)
- `~/domains/digitopub.com/public_html/.htaccess`:
  - `PassengerAppRoot /home/u391496830/domains/digitopub.com/nodejs`
  - `PassengerAppType node`, `PassengerStartupFile server.js`, `PassengerBaseURI /`
  - `PassengerRestartDir .../nodejs/tmp` → **restart = `touch ~/domains/digitopub.com/nodejs/tmp/restart.txt`**
  - `SetEnv NODE_OPTIONS --require .../.builds/config/preload-timestamp.js`, `TOKIO_WORKER_THREADS 2`
- `server.js` is **generated by Hostinger's build system** — a Next standalone launcher
  (`startServer` from `next/dist/server/lib/start-server`) with the full resolved `next.config`
  inlined. `package.json`'s `start` script is NOT used in production.

### Deployment mechanism (was undocumented)
Hostinger's managed "builds" pipeline, rooted at `~/domains/digitopub.com/public_html/.builds/`:
- `source/repository` — checked-out source; `last-source/` — previous source snapshot
- `config/.env` — **the real production env file** (hPanel-managed)
- `config/preload-timestamp.js`, `logs/<uuid>/` — build logs
- Output: standalone Next build deployed to `~/domains/digitopub.com/nodejs/`
- **Last build: 2026-06-25.** Production is ~6 weeks behind `main` (predates the OTP email
  delivery work, verification-code widening, and everything since).

### Resources
- Host box: 64 cores, ~500 GB RAM (shared). No cgroup memory cap visible; `ulimit -m` unlimited,
  8192 open files, stack 8 MB.
- No `crontab` binary on PATH → **scheduling must go through hPanel Cron Jobs or an external
  HTTP scheduler (cron-job.org)**. This validates the Phase 1 decision to expose all recurring
  jobs as Bearer-protected HTTP endpoints.

### Outbound IP (for SiteGround Remote-MySQL whitelist)
- `curl ifconfig.me` from the Hostinger box returned an **IPv6 address**:
  `2a02:4780:27:1566:0:1755:c47e:1`. If SiteGround whitelisting is IPv4-based, the effective
  IPv4 egress needs separate confirmation (`curl -4 ifconfig.me`) before relying on direct
  OJS MySQL from production. OJS DB currently connects — whatever whitelist exists is working —
  but treat whitelist flap as a top risk (see risk register in the overhaul plan).
- MariaDB client 11.8.8 available on the host.

### Production env gaps (var names read from `.builds/config/.env`; values untouched)
Present: `DATABASE_*`, `JWT_SECRET`, `NEXT_PUBLIC_APP_URL`, `ALLOWED_ORIGINS`, `OJS_DATABASE_*`
(direct-MySQL mode), `OJS_BASE_URL`, `NEXT_PUBLIC_OJS_BASE_URL`, `OJS_API_KEY`,
`OJS_BRIDGE_URL`, `OJS_PDF_BRIDGE_URL`, `OJS_SYNC_ENABLED`, `OJS_SYNC_INTERVAL_MINUTES`,
`SMTP_*`, `OTP_DELIVERY_METHOD`, `AUDIO_STORAGE_DIR`, upload vars, seeding credentials,
`ALLOW_PROD_DB_INIT`.

**Missing in production** (needed before the corresponding features work there):
- `CRON_SECRET` — without it, `/api/ojs/sync` and the new `/api/cron/*` endpoints reject everything
- All UIET-P1 identity secrets: `IDENTITY_COOKIE_SECRET`, `ORCID_CLIENT_ID`, `ORCID_CLIENT_SECRET`,
  `ORCID_STATE_SECRET`, `ORCID_REDIRECT_URI`, `EVENT_IP_HASH_SALT_SEED`, `UIET_P1_ENABLED`,
  `ENABLE_ORCID_OJS_BACKFILL`
- `PUBLIC_OJS_BASE_URL`, `S3_*` (if S3 audio storage is intended), `STRIPE_*` (if billing goes live)

## SiteGround (OJS)

- **OJS 3.5.0.2** (`dbscripts/xml/version.xml`), installed at
  `~/www/submitmanager.com/public_html/`, `base_url = https://journals.digitopub.com`
  (**cutover step 4 has happened**). `submitmanager.com` now 301s to the subdomain
  (verified from the Hostinger box — plain server-side HEAD was NOT WAF-blocked at the root;
  the image-proxy remains justified for asset paths until separately re-tested).
- PHP 8.2.33 CLI, MySQL 8.4.10, DB `dbkgvcunttgs97` on `localhost` (app-local; digitopub
  reaches it via SiteGround Remote MySQL + whitelist).
- No `crontab` binary here either → OJS scheduled tasks run via Site Tools cron.

### Incremental-sync go/no-go (Phase 1.3) — **GO**
| Table | Timestamp column | Verified |
| --- | --- | --- |
| `submissions` | `last_modified` (datetime, nullable) | ✔ (NOT `last_modification` as OJS 3.3 docs suggest) |
| `publications` | `last_modified` (datetime) | ✔ — `MAX(last_modified)` = 2026-08-05, actively maintained |
| `issues` | `last_modified` (datetime) | ✔ |
| `journals` | **none** | ✔ confirmed absent → fingerprint approach stands |

### Scale (right-sizes everything)
- **13 journals, 98 submissions, 40 users.** Tiny. Full-refresh aggregate queries are trivially
  cheap; the watermark is an optimization, not a necessity. GROUP_CONCAT fingerprint over 13
  journals is nowhere near truncation limits (test the >50 case anyway).

## Consequences applied in this phase
1. Node pinned to 20.19.4 (`.nvmrc`, `.node-version`, `engines >=20`).
2. `npx` removed from `package.json` runtime scripts (`npx` does not exist on the production PATH).
3. `.env.production.template` refreshed to mirror the real production var set + documented gaps.
4. Actual deploy process documented in [`deploy.md`](./deploy.md).
