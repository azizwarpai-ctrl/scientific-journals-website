# Task Board — Scholar Subdomain Initiative

Single source of truth for task status. Update the `status` column as part of every PR.

Status values: `todo` · `ready` · `in-progress` · `in-review` · `blocked-on-ops` · `done`

| id | title                                                              | owner   | depends-on | status | acceptance criteria |
| -- | ------------------------------------------------------------------ | ------- | ---------- | ------ | ------------------- |
| T0 | Docs-only kickoff PR (this directory)                              | [repo]  | —          | done   | `docs/scholar-subdomain/{README,data-flow,TASKS,working-agreement}.md` present; PR has no code changes; human approves before any task starts. |
| T1 | OJS host single-source + apex canonical → subdomain                | [repo]  | T0         | in-review | (1) No `submitmanager.com` literal remains in `app/**` or `src/**` (grep confirms only a single docstring mention in `ojs-config.ts`). (2) All OJS URL construction routes through `getOjsBaseUrl` / `getPublicOjsBaseUrl` / `buildOjsArticleLandingUrl` / `getOjsHostnames` / `normalizeOjsAssetUrl`. (3) Apex `app/journals/[id]/articles/[publicationId]/page.tsx` sets `<link rel="canonical">` to the subdomain landing URL built from the real `submissionId` + `journalUrlPath` (NOT `[publicationId]`). (4) `EMIT_SCHOLAR_CITATION_META` gated via `shouldEmitScholarCitationMeta()`, default `false`; no `citation_*` emitted on apex. (5) Unit tests assert canonical host = `journals.digitopub.com` and the flag defaults to `false`. (6) `tsc --noEmit` clean; no NEW lint warnings or errors (pre-existing baseline unchanged — see T7/T8). (7) Rollback note: revert single PR; runtime env unchanged. |
| T2 | Image pipeline at the root — one resolver + one render path        | [repo]  | T0         | in-review | **Inventory found PR #108 already consolidated the bulk of this.** Single resolver (`parseOjsFilename` + `buildOjsAssetUrl`/`buildOjsPublicUrl`) in `src/features/ojs/utils/ojs-asset-url.ts`; single render path (`<OjsImage>` → `/api/image-proxy`). Covers, editorial photos (`MemberPhoto`), advisory board, and Highlights (`HighlightCard`) all route through `<OjsImage>`. `resolveProfileImageUrl` delegates to the shared resolver. `next.config.mjs` `remotePatterns` covers both hosts. No bare `<img>` JSX outside `ojs-image.tsx`; no hand-built `${X}/public/...` URLs in active code. This T2 PR adds: (1) **T7 carry-over** — `parseOjsFilename` now applies an `IMAGE_FILENAME_SHAPE` extension/prefix guard to every extracted candidate (JSON `name`/`uploadName`, PHP-serialized `name`/`uploadName`, plain string), so a corrupted settings row with `name: "<script>"` / `""` / `"justaword"` returns null rather than flowing into URL construction. (2) **Repository guard test** (`tests/unit/ojs-image-pipeline-guard.test.ts`) — fails the suite if anyone reintroduces a bare `<img>` outside `ojs-image.tsx` or hand-builds a `${X}/public/(journals|site)/` URL outside the shared resolver. (3) Three regression tests for the new shape guard. Suite: 517 → 521. Lint: 0 errors / 69 warnings (baseline unchanged). No host literals reintroduced. Rollback: single revert. |
| T3 | HTML-entity link fix in custom blocks                              | [repo]  | T0         | in-review | **Inventory confirms BOTH halves already fixed end-to-end** by commits `76d7f19` (cheerio `.attr()` in `extractCardFields` for `href` + `src`) and `6653d98` (shared `decodeHtmlEntities` in `src/lib/html-utils.ts`); `extractLinksFromBiography` pre-decodes the bio via `decodeHtmlEntities` before regex. Regression tests exist (`tests/unit/custom-blocks-service.test.ts:209–257` covers `&amp;`/`&#38;`/`&#x26;` for href and `&amp;` for src; `tests/unit/editorial-board-bio-links.test.ts` covers all three entity forms in both Scholar and Scopus URLs, plus the exact iJMP target URL `?hl=en&user=jQuDgSkAAAAJ`). This T3 PR adds the **lock**: `tests/unit/html-attr-extraction-guard.test.ts` fails the suite if anyone reintroduces a raw regex literal of the form `/href="([^"]+)"/` or `/src=(...)/` anywhere in `app/**` or `src/**`. Includes a self-test that pins six positive/negative cases so the guard regex itself can't silently weaken. Suite: 521 → 529. Lint: 0 errors / 69 warnings (unchanged). Rollback: single revert. |
| T4 | Generate cutover artifacts                                         | [repo]  | T0         | todo   | (1) `docs/ops/submitmanager-301.htaccess` — host-conditional 301 from `submitmanager.com` → `https://journals.digitopub.com$1`, with comments explaining each directive. (2) `docs/ops/verify-cutover.sh` — executable battery covering: DNS A record, HTTPS cert validity, OJS landing emits `citation_*` on subdomain, PDF 200 `application/pdf` on subdomain, robots crawlable, `submitmanager.com` → 301 → subdomain, apex `<link rel="canonical">` points at subdomain, apex emits no `citation_*`. (3) Script is idempotent, exits non-zero on any red signal, prints a colored matrix. (4) No infra performed. |
| T5 | Cutover execution                                                  | [ops]   | T4         | blocked-on-ops | Operator runs the runbook step by step; each step verified with `verify-cutover.sh` slice and confirmed in the PR thread. (1) DNS A record live. (2) SiteGround alias + Let's Encrypt SSL serving 200 on subdomain. (3) OJS `base_url` flipped, caches cleared, emitted URLs all `journals.digitopub.com`. (4) Apache 301 applied to `submitmanager.com`. (5) DOIs re-deposited at Crossref; sample DOI resolves to subdomain. (6) Rollback note: revert OJS `base_url`, remove 301, restore DNS. |
| T6 | Finalize — apex deploy, GSC, audit, monitoring                     | [ops] + [repo] | T5  | todo   | (1) Apex env `OJS_BASE_URL` / `PUBLIC_OJS_BASE_URL` / `NEXT_PUBLIC_OJS_BASE_URL` deployed → subdomain. (2) GSC property added; OJS sitemap submitted. (3) OJS log hygiene done (no stale `submitmanager.com` references in error pages). (4) Open-access audit of all 12 journals: each has `citation_*` + 200 PDF on subdomain. (5) `verify-cutover.sh` matrix fully green. (6) Scholar recrawl monitored; first new-host citation observed and recorded in this file. |
| T7 | Fix pre-existing `parseOjsFilename` test fixture mismatch          | [repo]  | —          | in-review | Root cause: **stale test**. Commit `0437c04` made `name` a recognized (and now preferred) key alongside `uploadName`; the old fixture `{"name":"something",...}` was written when only `uploadName` was recognized, so it no longer exercises its stated intent. Fixture updated to a JSON object that carries neither key (`{"label":"something","value":42}`), preserving the original assertion. Suite now 515/515. No parser code change. Note for T2 scoping: `parseOjsFilename` does not validate the extracted `name`/`uploadName` value as filename-shaped — in real OJS data it always is, but the consolidated resolver could optionally extension-check before trusting it. |
| T8 | Remove stray `test-parse.js` from repo root                        | [repo]  | —          | in-review | Deleted the leftover scratch script from commit `0437c04`; its logic is already covered by `parseOjsFilename` + tests. `bun run lint` is now `0 errors, 76 warnings`; suite still 515/515. |

## How to update this board

- Change `status` in the same PR that advances the task.
- If a task uncovers new scope, **add a new row** (T7+); do NOT widen an existing
  task. The new row links to the PR that surfaced it.
- `blocked-on-ops` means: repo work is complete (or N/A), waiting on a human
  operator confirmation. Do not progress past this status from the repo side.
- `done` requires: merged PR + human approval recorded + verification evidence
  attached.
